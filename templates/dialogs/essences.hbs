<form class="dialog-form">
    <div class="dialog-row">
        <label for="system.water">{{localize 'roe.essences.water'}}</label>
        <input name="system.water" type="number" value="{{actor.system.water}}" data-dtype="Number" />
    </div>

    <div class="dialog-row">
        <label for="system.air">{{localize 'roe.essences.air'}}</label>
        <input name="system.air" type="number" value="{{actor.system.air}}" data-dtype="Number" />
    </div>  

    <div class="dialog-row">
        <label for="system.fire">{{localize 'roe.essences.fire'}}</label>
        <input name="system.fire" type="number" value="{{actor.system.fire}}" data-dtype="Number" />
    </div>

    <div class="dialog-row">
        <label for="system.earth">{{localize 'roe.essences.earth'}}</label>
        <input name="system.earth" type="number" value="{{actor.system.earth}}" data-dtype="Number" />
    </div>  

    <div class="dialog-row">
        <label for="system.light">{{localize 'roe.essences.light'}}</label>
        <input name="system.light" type="number" value="{{actor.system.light}}" data-dtype="Number" />
    </div>

    <div class="dialog-row">
        <label for="system.darkness">{{localize 'roe.essences.darkness'}}</label>
        <input name="system.darkness" type="number" value="{{actor.system.darkness}}" data-dtype="Number" />
    </div>  

    <div class="dialog-row">
        <label for="system.moon">{{localize 'roe.essences.moon'}}</label>
        <input name="system.moon" type="number" value="{{actor.system.moon}}" data-dtype="Number" />
    </div>

    <div class="dialog-row">
        <label for="system.nature">{{localize 'roe.essences.nature'}}</label>
        <input name="system.nature" type="number" value="{{actor.system.nature}}" data-dtype="Number" />
    </div>  
</form>