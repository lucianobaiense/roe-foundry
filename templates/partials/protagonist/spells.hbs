<section>
    <div class="flex">
        <h3 class="mb-2">{{localize 'roe.general.spells'}}</h3>
        <a class="item-create" data-type="spell" title="{{localize 'roe.general.add'}}">
            <i class="fas fa-plus"></i>
        </a>
    </div>

    {{#each spells as |spell|}}
        <div class="item flex" data-id="{{spell._id}}">
            <input class="item-edit-inline" data-field="name" type="text" value="{{spell.name}}" data-dtype="String"/>
            <input class="item-edit-inline" data-field="system.enchants" type="text" value="{{spell.system.enchants}}" data-dtype="String"/>

            <a class="item-roll" data-type="spell" data-name="{{spell.name}}">
                <i class="fas fa-dice"></i>
            </a>

            <a class="item-edit-popup" title="{{localize 'roe.general.edit'}}">
                <i class="fas fa-edit"></i>
            </a>

            <a class="item-delete" title="{{localize 'roe.general.delete'}}">
                <i class="fas fa-trash"></i>
            </a>
        </div>
        <div class="flex mt-1 mb-2">
            {{#if spell.system.action}}
                <p class="bg-stone-400 mr-1 px-1">{{spell.system.action}}</p>
            {{/if}}

            {{#if spell.system.cost}}
                <p class="bg-stone-400 mr-1 px-1">{{spell.system.cost}}</p>
            {{/if}}

            {{#if spell.system.duration}}
                <p class="bg-stone-400 mr-1 px-1">{{spell.system.duration}}</p>
            {{/if}}

            {{#if spell.system.range}}
                <p class="bg-stone-400 mr-1 px-1">{{spell.system.range}}</p>
            {{/if}}

            {{#if spell.system.target}}
                <p class="bg-stone-400 mr-1 px-1">{{spell.system.target}}</p>
            {{/if}}         
        </div>
    {{/each}}
</section>